void main() {

  // final means the variable can be assigned only once.
  final name = "Flutter";

  // Print the value.
  print(name);

  // name = "Dart";
  // print(name);

  // This is NOT allowed because final variables cannot be assigned a new value.

}